# GameLink - P2P Gaming LAN

n2n P2P VPN을 이용한 게이밍 LAN 자동 구축 도구입니다.
설치 스크립트 하나로 VPN 연결부터 시스템 트레이 앱까지 자동으로 세팅됩니다.

## 구조

```
[클라이언트 PC]                    [슈퍼노드 서버]
 ┌──────────────┐                 ┌──────────────┐
 │ GameLink.exe │                 │  supernode    │
 │ edge.exe     │── UDP P2P ────▶│  (UDP 7654)   │
 │ TAP 드라이버  │                 └──────────────┘
 │ VPN IP       │                        ▲
 │ 10.0.x.x     │                        │
 └──────────────┘                        │
                                         │
[클라이언트 PC]                          │
 ┌──────────────┐                        │
 │ GameLink.exe │── UDP P2P ─────────────┘
 │ edge.exe     │
 │ VPN IP       │
 │ 10.0.x.x     │
 └──────────────┘
```

클라이언트끼리는 슈퍼노드를 통해 서로를 발견한 뒤 **직접 P2P 통신**합니다.

---

## ⚠️ 사전 준비: 슈퍼노드 서버

클라이언트 설치 전에 **슈퍼노드 서버가 먼저 구축되어 있어야** 합니다.
슈퍼노드는 클라이언트들이 서로를 찾을 수 있도록 중개하는 서버입니다.

> 서버 구축 방법은 **[SERVER_SETUP.md](SERVER_SETUP.md)** 를 참고하세요.

---

## 클라이언트 설치 (Windows)

### 요구사항

- Windows 10 / 11 (64-bit)
- 관리자 권한
- .NET Framework 4.x (기본 설치됨)
- 인터넷 연결

### 빠른 시작

**1. 설정 파일 준비**

`env.ini.example`을 복사하여 `env.ini`를 만들고 실제 값으로 수정합니다.

```powershell
Copy-Item env.ini.example env.ini
notepad env.ini
```

`env.ini` 내용:
```ini
[Settings]
SuperNode=your.supernode.com:7654
Community=YOUR_COMMUNITY
EncryptKey=YourSecretKey123
```

> 모든 클라이언트는 **같은 Community, EncryptKey** 값을 사용해야 합니다.

**2. 설치 스크립트 실행**

PowerShell을 **관리자 권한**으로 열고 실행합니다.

```powershell
Set-ExecutionPolicy Bypass -Scope Process -Force; .\install_edge.ps1
```

설치 중 **회원 고유번호**를 입력하면 VPN IP가 자동 할당됩니다.

### 설치 스크립트가 하는 일

1. `C:\n2n` 디렉토리 생성
2. n2n edge.exe 바이너리 설치 (로컬 → GitHub 자동 다운로드)
3. TAP 네트워크 드라이버 설치
4. Windows 방화벽 UDP 규칙 추가
5. 회원번호 입력 → config.ini 생성 + GameLink.exe 컴파일

---

## 사용법

### 설치 완료 후 폴더 구조

`C:\n2n` 폴더에 다음 파일이 생성됩니다.

| 파일 | 설명 |
|------|------|
| `GameLink.exe` | 시스템 트레이 앱 (더블클릭으로 실행) |
| `config.ini` | VPN 설정 파일 (메모장으로 수정 가능) |
| `edge.exe` | n2n VPN 엔진 |

### GameLink.exe 사용법

- **더블클릭** → 자동 VPN 연결 + 시스템 트레이 상주
- **트레이 좌클릭** → 실시간 로그 창
- **트레이 우클릭** → 메뉴
  - 연결 / 재연결 / 연결 해제
  - 로그 보기
  - config.ini 열기
  - 종료

### 트레이 아이콘 상태

| 색상 | 상태 |
|------|------|
| ⚪ 회색 | 대기 / 연결 해제 |
| 🟡 노랑 | 연결 시도 중 |
| 🟢 초록 | 연결 성공 |
| 🔴 빨강 | 연결 끊김 / 오류 |

---

## IP 할당 규칙

회원 고유번호(1~65024)에 따라 VPN IP가 자동 계산됩니다.

```
VPN IP = 10.0.{octet3}.{octet4}

octet3 = (회원번호 - 1) / 254  (소수점 버림)
octet4 = (회원번호 - 1) % 254 + 1
```

| 회원번호 | VPN IP |
|---------|--------|
| 1 | 10.0.0.1 |
| 27 | 10.0.0.27 |
| 254 | 10.0.0.254 |
| 255 | 10.0.1.1 |
| 300 | 10.0.1.46 |
| 1024 | 10.0.4.3 |

설치 후 `C:\n2n\config.ini`의 `MemberID` 값을 수정하면 IP를 변경할 수 있습니다.

---

## config.ini 예제

```ini
[VPN]
; GameLink 설정 파일

; 슈퍼노드 주소 (변경 금지)
SuperNode=your.supernode.com:7654

; 커뮤니티 이름 (변경 금지)
Community=MY_COMMUNITY

; 암호화 키 (변경 금지)
Key=YourSecretKey123

; 본인 회원 고유번호 (잘못 입력했으면 여기서 수정)
MemberID=27
```

---

## edge.exe 수동 설치

자동 다운로드가 실패할 경우:

1. [lucktu/n2n Windows 빌드](https://github.com/lucktu/n2n/tree/master/Windows) 페이지 접속
2. 파일명에 `v3`, `x64`가 포함된 `.zip` 다운로드
3. zip 안의 `edge.exe`를 스크립트 폴더에 복사
4. 스크립트 재실행

---

## 문제 해결

| 증상 | 해결 |
|------|------|
| GameLink.exe 실행 안 됨 | 우클릭 → 관리자 권한으로 실행 |
| 트레이 아이콘이 빨간색 | 서버 상태 확인, 인터넷 연결 확인 |
| 상대방 PC가 안 보임 | 양쪽 모두 같은 Community, Key 사용하는지 확인 |
| TAP 드라이버 오류 | PC 재부팅 후 재시도 |
